# ==========================================
# FINAL RENDER DEPLOYMENT ENVIRONMENT (PRODUCTION)
# ==========================================

# 1. DATABASE
# Must be your MongoDB Atlas connection string. 
# Make sure your IP is whitelisted (0.0.0.0/0) in MongoDB Atlas settings.
MONGODB_URI=mongodb+srv://eadvocate:Advocate%401.@cluster0.rlkrhz6.mongodb.net/?appName=Cluster0

# 2. EMAIL (SMTP)
# Using your neurogenix187@gmail.com with App Password
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=neurogenix187@gmail.com
SMTP_PASS=jnqj codi oxea zmec

# 3. APP SETTINGS
# Port for Render (they automatically detect this, but 10000 is default)
PORT=10000
NODE_ENV=production

# 4. TOKEN SECURITY (Recommended to add)
# While current code uses ID-based tokens, adding this keeps the system ready for JWT.
JWT_SECRET=eadvocate_secret_key_2024_secure

# 5. SITE URL (Optional)
# Set this to your Render .onrender.com URL once the first deployment is done
# SITE_URL=https://eadvocate-service.onrender.com
